# 微信小游戏客户端技术路线文档：Custom Mahjong Game Client

> 用途：本文件用于指导 Codex 在 `game-client/` 或 `frontend/` 文件夹中搭建麻将游戏客户端。  
> 目标：按照“微信小游戏”而不是“普通小程序页面”的方向推进，使用游戏引擎实现麻将牌桌、动画、音效、输入交互、实时对局同步和回放展示。  
> 推荐技术栈：Cocos Creator 3.x + TypeScript + 微信小游戏发布 + WebSocket + 后端权威状态同步。  
> 范围：本文件只覆盖游戏客户端，不覆盖后端规则系统，也不覆盖 AI 模型训练系统。

---

## 0. 核心定位

本项目要做的是“麻将游戏页面”，因此前端不应再按普通小程序页面应用推进，而应按**微信小游戏客户端**推进。

普通小程序路线更适合：

```text
表单
列表
个人中心
管理页面
轻交互业务系统
```

麻将游戏客户端更适合：

```text
Canvas / WebGL 渲染
牌桌布局
出牌动画
摸牌动画
碰杠胡特效
音效系统
资源管理
游戏循环
实时 WebSocket 状态同步
```

因此推荐技术路线调整为：

```text
Cocos Creator 3.x + TypeScript
        ↓
构建为微信小游戏
        ↓
通过 HTTP / WebSocket 连接后端
        ↓
后端负责规则与状态
        ↓
AI 服务负责模型推理
```

---

## 1. 总体架构

完整系统架构：

```text
Cocos 微信小游戏客户端
  │
  ├── HTTP
  │     ├── 登录
  │     ├── 创建房间
  │     ├── 加入房间
  │     ├── 获取牌谱
  │     └── 获取配置
  │
  └── WebSocket
        ├── 房间状态同步
        ├── 对局动作提交
        ├── GAME_VIEW 推送
        ├── GAME_EVENTS 推送
        └── 断线重连

Node.js / TypeScript 后端
  ├── 用户与房间
  ├── 规则引擎
  ├── 状态流转
  ├── 结算
  ├── 牌谱保存
  └── AI Gateway

Python AI 推理服务
  ├── 加载训练模型
  ├── 接收 observation
  ├── 返回 action
  └── fallback
```

客户端只负责：

```text
1. 牌桌渲染；
2. 用户输入；
3. 动画表现；
4. 音效播放；
5. 状态展示；
6. WebSocket 通信；
7. 回放播放；
8. 本地 UI 状态。
```

客户端不负责：

```text
1. 发牌；
2. 洗牌；
3. 判断动作是否合法；
4. 判断能否胡牌；
5. 番型计算；
6. 分数结算；
7. AI 推理；
8. 保存完整 GameState；
9. 查看其他玩家暗手牌。
```

核心原则：

```text
后端是唯一裁判；
客户端只展示后端返回的 PlayerGameView；
客户端只提交动作意图；
所有动作最终由后端规则引擎校验；
客户端不能依赖本地规则推演结果。
```

---

## 2. 推荐技术栈

### 2.1 客户端

```text
Cocos Creator 3.x
TypeScript
微信小游戏构建目标
Cocos UI 系统
Cocos Tween / Animation
Cocos AudioSource
Cocos Asset Bundle
Cocos Prefab
Cocos EventTarget
WebSocket
HTTP Request
对象池 Object Pool
有限状态机 FSM
```

### 2.2 后端对接

```text
HTTP API：
  登录、房间、牌谱、配置

WebSocket：
  实时对局、动作提交、状态同步

协议格式：
  JSON message
```

### 2.3 为什么选择 Cocos Creator

选择 Cocos Creator 的原因：

```text
1. 麻将是典型 2D 棋牌类游戏，Cocos 很适合；
2. 支持 TypeScript，便于与后端协议类型对齐；
3. 有场景、节点、预制体、动画、音频、资源管理体系；
4. 可以直接构建微信小游戏；
5. 比纯 Canvas 手写更容易维护；
6. 比 Unity 更轻，更适合 2D 麻将；
7. 比 Taro/React 更适合游戏表现和动画。
```

---

## 3. 仓库定位

建议三个仓库或三个顶层文件夹为：

```text
mahjong-game-client     # Cocos Creator 微信小游戏客户端
mahjong-backend         # Node.js / TypeScript 后端
mahjong-ai-training     # Python 强化学习训练
```

如果目前已有 `frontend/` 文件夹，也可以直接将其改造成：

```text
frontend/               # Cocos Creator 项目，而不是 Taro 小程序项目
```

建议命名：

```text
game-client/
```

而不是：

```text
miniprogram/
```

因为当前项目本质是小游戏客户端。

---

## 4. 推荐目录结构

Codex 应在游戏客户端文件夹中生成或维护如下结构：

```text
game-client/
├── README.md
├── project.json
├── tsconfig.json
├── settings/
├── assets/
│   ├── scenes/
│   │   ├── Boot.scene
│   │   ├── Login.scene
│   │   ├── Lobby.scene
│   │   ├── Room.scene
│   │   ├── Game.scene
│   │   ├── Result.scene
│   │   └── Replay.scene
│   ├── scripts/
│   │   ├── app/
│   │   │   ├── App.ts
│   │   │   ├── AppConfig.ts
│   │   │   ├── GameEvents.ts
│   │   │   └── Constants.ts
│   │   ├── core/
│   │   │   ├── BaseScene.ts
│   │   │   ├── BaseView.ts
│   │   │   ├── EventBus.ts
│   │   │   ├── StateMachine.ts
│   │   │   ├── ObjectPool.ts
│   │   │   └── Timer.ts
│   │   ├── network/
│   │   │   ├── HttpClient.ts
│   │   │   ├── WsClient.ts
│   │   │   ├── ApiRoutes.ts
│   │   │   ├── Protocol.ts
│   │   │   └── ReconnectPolicy.ts
│   │   ├── auth/
│   │   │   ├── AuthManager.ts
│   │   │   ├── AuthApi.ts
│   │   │   └── AuthTypes.ts
│   │   ├── room/
│   │   │   ├── RoomManager.ts
│   │   │   ├── RoomApi.ts
│   │   │   ├── RoomSceneController.ts
│   │   │   └── RoomTypes.ts
│   │   ├── game/
│   │   │   ├── GameManager.ts
│   │   │   ├── GameController.ts
│   │   │   ├── GameTypes.ts
│   │   │   ├── GameProtocol.ts
│   │   │   ├── GameViewMapper.ts
│   │   │   └── GameActionBuilder.ts
│   │   ├── game/views/
│   │   │   ├── GameBoardView.ts
│   │   │   ├── TileView.ts
│   │   │   ├── HandView.ts
│   │   │   ├── DiscardView.ts
│   │   │   ├── MeldView.ts
│   │   │   ├── PlayerSeatView.ts
│   │   │   ├── ActionPanelView.ts
│   │   │   ├── ScorePanelView.ts
│   │   │   ├── CenterTableView.ts
│   │   │   ├── ResultPanelView.ts
│   │   │   └── ConnectionStatusView.ts
│   │   ├── replay/
│   │   │   ├── ReplayManager.ts
│   │   │   ├── ReplayController.ts
│   │   │   ├── ReplayPlayer.ts
│   │   │   └── ReplayTypes.ts
│   │   ├── ui/
│   │   │   ├── Toast.ts
│   │   │   ├── LoadingMask.ts
│   │   │   ├── Dialog.ts
│   │   │   ├── ButtonEx.ts
│   │   │   └── PageTransition.ts
│   │   ├── assets/
│   │   │   ├── AssetManager.ts
│   │   │   ├── TileAssetMap.ts
│   │   │   └── BundleLoader.ts
│   │   ├── audio/
│   │   │   ├── AudioManager.ts
│   │   │   └── AudioKeys.ts
│   │   └── utils/
│   │       ├── TileUtils.ts
│   │       ├── ActionUtils.ts
│   │       ├── Storage.ts
│   │       ├── Logger.ts
│   │       └── Id.ts
│   ├── prefabs/
│   │   ├── Tile.prefab
│   │   ├── PlayerSeat.prefab
│   │   ├── ActionPanel.prefab
│   │   ├── Meld.prefab
│   │   ├── ResultPanel.prefab
│   │   ├── LoadingMask.prefab
│   │   └── Toast.prefab
│   ├── textures/
│   │   ├── tiles/
│   │   │   ├── tile_0.png
│   │   │   ├── tile_1.png
│   │   │   └── tile_back.png
│   │   ├── table/
│   │   ├── ui/
│   │   └── effects/
│   ├── audio/
│   │   ├── discard.mp3
│   │   ├── pong.mp3
│   │   ├── kong.mp3
│   │   ├── win.mp3
│   │   └── button.mp3
│   ├── animations/
│   └── resources/
├── build/
├── docs/
│   ├── game_client_architecture.md
│   ├── scene_flow.md
│   ├── websocket_protocol.md
│   ├── prefab_spec.md
│   ├── asset_spec.md
│   └── deployment_wechat_game.md
└── tests/
    ├── protocol.test.ts
    ├── tile-utils.test.ts
    ├── action-utils.test.ts
    └── game-view-mapper.test.ts
```

---

## 5. 场景设计

### 5.1 Boot Scene

职责：

```text
1. 初始化全局配置；
2. 初始化日志；
3. 初始化资源系统；
4. 初始化网络模块；
5. 读取本地 token；
6. 判断进入 Login 或 Lobby。
```

启动流程：

```text
Boot
  ├── 如果无 token → Login
  └── 如果有 token → 尝试恢复登录 → Lobby
```

### 5.2 Login Scene

职责：

```text
1. 调用微信登录；
2. 获取临时 code；
3. 调用后端登录接口；
4. 保存 token 和用户信息；
5. 进入 Lobby。
```

需要支持：

```text
真实微信登录；
开发 mock login；
登录失败重试；
loading 状态。
```

### 5.3 Lobby Scene

职责：

```text
1. 展示用户信息；
2. 创建房间；
3. 输入房间号加入；
4. 查看历史牌谱；
5. 进入设置。
```

按钮：

```text
创建房间
加入房间
历史战绩
设置
```

### 5.4 Room Scene

职责：

```text
1. 展示房间号；
2. 展示 4 个座位；
3. 房主添加 AI；
4. 玩家准备；
5. 房主开始游戏；
6. 监听 ROOM_UPDATE；
7. 游戏开始后进入 Game Scene。
```

### 5.5 Game Scene

核心场景。

职责：

```text
1. 订阅房间 WebSocket；
2. 展示牌桌；
3. 展示 PlayerGameView；
4. 处理用户点击手牌；
5. 处理吃碰杠胡过动作；
6. 接收 GAME_VIEW；
7. 播放 GAME_EVENTS 动画；
8. 显示 AI 思考状态；
9. 显示连接状态；
10. 终局后进入 Result Scene。
```

### 5.6 Result Scene

职责：

```text
1. 展示本局结果；
2. 展示每家分数；
3. 展示赢家；
4. 返回房间；
5. 再来一局；
6. 查看回放。
```

### 5.7 Replay Scene

职责：

```text
1. 加载牌谱；
2. 按 step 重放；
3. 支持暂停、继续、上一手、下一手；
4. 复用 GameBoardView；
5. 不允许提交动作。
```

---

## 6. 客户端模块分层

### 6.1 Manager 层

Manager 是跨场景的全局服务。

```text
AuthManager：
  登录、token、用户信息。

NetworkManager：
  HTTP 和 WebSocket 初始化。

RoomManager：
  当前房间状态。

GameManager：
  当前游戏视角、事件队列、动作提交。

AudioManager：
  音效和音乐。

AssetManager：
  资源加载、牌图映射、bundle 加载。

ReplayManager：
  牌谱数据、回放状态。
```

### 6.2 Controller 层

Controller 负责场景逻辑。

```text
LoginSceneController
LobbySceneController
RoomSceneController
GameController
ReplayController
```

例如 `GameController`：

```text
1. 初始化 GameBoardView；
2. 绑定 WebSocket 事件；
3. 监听用户动作；
4. 调用 GameManager.submitAction；
5. 接收状态后驱动 View 刷新；
6. 控制动画播放顺序；
7. 处理终局跳转。
```

### 6.3 View 层

View 负责节点和 UI 表现，不直接访问后端。

```text
TileView
HandView
DiscardView
MeldView
PlayerSeatView
ActionPanelView
GameBoardView
ResultPanelView
```

View 只做：

```text
显示；
动画；
点击事件抛出；
局部状态表现。
```

### 6.4 Model / Types 层

负责协议类型定义：

```text
TileId
GameAction
PlayerGameView
RoomView
WsMessage
GameEvent
ScoreResult
ReplayStep
```

必须与后端协议保持一致。

---

## 7. 核心数据流

### 7.1 进入游戏

```text
RoomScene 点击开始游戏
  ↓
后端 startGame
  ↓
返回 gameId
  ↓
进入 GameScene
  ↓
WsClient subscribe room
  ↓
HTTP 拉取 getGameView
  ↓
GameBoardView 渲染牌桌
```

### 7.2 玩家出牌

```text
用户点击 TileView
  ↓
HandView 抛出 tileSelected
  ↓
GameController 判断该 tile 是否存在于 legalActions 的 DISCARD 中
  ↓
如果合法，则再次点击或点击出牌按钮
  ↓
GameManager.submitAction
  ↓
WsClient 发送 GAME_ACTION
  ↓
后端校验
  ↓
后端广播 GAME_VIEW / GAME_EVENTS
  ↓
GameManager 更新 view
  ↓
GameBoardView 刷新
  ↓
播放出牌动画
```

注意：

```text
客户端只检查 legalActions 以决定 UI 是否可点；
不自己判断规则；
后端仍然是唯一裁判。
```

### 7.3 AI 行动

```text
后端发现当前玩家是 AI
  ↓
后端调用 AI 推理服务
  ↓
AI 返回动作
  ↓
后端校验并执行
  ↓
后端广播 GAME_VIEW / GAME_EVENTS
  ↓
客户端播放 AI 思考和出牌动画
```

客户端不调用 AI 服务。

### 7.4 断线重连

```text
WebSocket 断开
  ↓
ConnectionStatusView 显示“重连中”
  ↓
WsClient 按重连策略重连
  ↓
重连成功后重新 subscribe room
  ↓
HTTP 拉取最新 PlayerGameView
  ↓
覆盖本地 view
  ↓
恢复牌桌
```

---

## 8. 后端协议对齐

客户端必须与后端保持统一协议。

### 8.1 HTTP API

```text
POST /api/auth/wechat-login
POST /api/rooms
POST /api/rooms/:roomId/join
POST /api/rooms/:roomId/leave
POST /api/rooms/:roomId/add-ai
POST /api/rooms/:roomId/start
GET  /api/rooms/:roomId
GET  /api/games/:gameId/view
POST /api/games/:gameId/actions
GET  /api/replays/:gameId
```

### 8.2 WebSocket 连接

```text
wss://your-domain.com/ws?token=<jwt>
```

本地：

```text
ws://localhost:3000/ws?token=<jwt>
```

### 8.3 WebSocket 消息格式

```ts
export interface WsMessage<T = unknown> {
  type: string;
  requestId?: string;
  roomId?: string;
  gameId?: string;
  payload?: T;
  ts?: number;
}
```

### 8.4 客户端发送 GAME_ACTION

```json
{
  "type": "GAME_ACTION",
  "requestId": "req_001",
  "roomId": "room_001",
  "gameId": "game_001",
  "payload": {
    "type": "DISCARD",
    "tile": 12,
    "actionId": 12,
    "clientSeq": 35
  }
}
```

### 8.5 服务端推送 GAME_VIEW

```json
{
  "type": "GAME_VIEW",
  "roomId": "room_001",
  "gameId": "game_001",
  "payload": {
    "view": {
      "roomId": "room_001",
      "gameId": "game_001",
      "playerIndex": 0,
      "status": "PLAYING",
      "stepIndex": 35,
      "dealer": 0,
      "currentPlayer": 0,
      "scores": [0, 0, 0, 0],
      "self": {
        "hand": [1, 2, 3],
        "melds": [],
        "discards": []
      },
      "opponents": [],
      "legalActions": []
    }
  }
}
```

### 8.6 服务端推送 GAME_EVENTS

```json
{
  "type": "GAME_EVENTS",
  "roomId": "room_001",
  "gameId": "game_001",
  "payload": {
    "events": [
      {
        "type": "TILE_DISCARDED",
        "playerIndex": 0,
        "tile": 12
      }
    ]
  }
}
```

---

## 9. Cocos 组件设计

### 9.1 TileView

文件：

```text
assets/scripts/game/views/TileView.ts
```

职责：

```text
显示单张牌；
支持正面 / 背面；
支持选中状态；
支持禁用状态；
支持横向 / 竖向；
支持小 / 中 / 大尺寸；
播放选中动画；
播放出牌动画；
抛出点击事件。
```

接口建议：

```ts
export class TileView extends Component {
  setTile(tileId: number): void;
  setFaceDown(faceDown: boolean): void;
  setSelected(selected: boolean): void;
  setDisabled(disabled: boolean): void;
  playSelectAnimation(): void;
  playDiscardAnimation(target: Vec3): Promise<void>;
}
```

### 9.2 HandView

职责：

```text
展示自己的手牌；
排序手牌；
生成 TileView；
维护选中牌；
根据 legalDiscardTiles 控制可点状态；
处理点击；
抛出 discardRequested。
```

注意：

```text
HandView 不直接发送网络请求；
只向 GameController 抛事件。
```

### 9.3 DiscardView

职责：

```text
显示某个玩家的弃牌；
按行列布局；
高亮最新弃牌；
支持回放时逐步添加。
```

### 9.4 MeldView

职责：

```text
显示吃、碰、杠后的明牌组合；
支持横向展示；
支持来自哪个玩家的标识。
```

### 9.5 PlayerSeatView

职责：

```text
显示玩家昵称；
显示分数；
显示是否 AI；
显示是否离线；
显示手牌数量；
显示明牌和弃牌；
显示当前行动高亮。
```

### 9.6 ActionPanelView

职责：

```text
根据 legalActions 显示按钮；
胡、碰、杠、吃、过；
点击后抛出 actionSelected；
提交中禁用按钮；
后端更新后刷新。
```

### 9.7 GameBoardView

职责：

```text
组合所有子视图；
渲染完整 PlayerGameView；
处理座位方向映射；
控制局内动画播放顺序；
展示局中提示。
```

---

## 10. 牌桌布局设计

### 10.1 基本布局

推荐横屏或竖屏均可，但麻将更建议横屏或横向适配。

横屏布局：

```text
┌──────────────────────────────────────────┐
│                 上家                     │
│                                          │
│ 左家              桌面中心             右家 │
│                                          │
│              自己弃牌区                  │
│              自己手牌区                  │
│              动作按钮区                  │
└──────────────────────────────────────────┘
```

竖屏布局：

```text
┌──────────────────────────────┐
│           上家                │
│ 左家       桌面       右家     │
│           自己弃牌区           │
│           自己手牌区           │
│           动作按钮区           │
└──────────────────────────────┘
```

### 10.2 座位映射

后端返回 seatIndex：

```text
0, 1, 2, 3
```

客户端需要将其映射到本地视角：

```text
自己：bottom
下家：right
对家：top
上家：left
```

函数：

```ts
function mapSeatToLocalPosition(selfIndex: number, targetIndex: number): LocalSeatPosition {
  const diff = (targetIndex - selfIndex + 4) % 4;
  if (diff === 0) return 'bottom';
  if (diff === 1) return 'right';
  if (diff === 2) return 'top';
  return 'left';
}
```

---

## 11. 动画系统

### 11.1 必须支持的基础动画

```text
手牌选中上移；
出牌移动到弃牌区；
摸牌插入手牌；
最新弃牌高亮；
吃碰杠弹字；
胡牌弹字；
结算面板弹出；
AI 思考提示。
```

### 11.2 动画播放原则

```text
后端状态是权威；
动画只是表现层；
如果动画丢失或中断，最终 view 仍要与后端一致；
收到新的 GAME_VIEW 后，必要时直接刷新到最新状态。
```

### 11.3 动画队列

建议实现事件动画队列：

```text
GAME_EVENTS
  ↓
AnimationQueue
  ↓
逐个播放 TILE_DISCARDED / MELD_CREATED / WIN_DECLARED
  ↓
播放完成后 apply GAME_VIEW
```

或者简化为：

```text
先更新 view，再播放轻量提示动画。
```

第一版建议采用简化方案，优先保证状态正确。

---

## 12. 音效系统

### 12.1 AudioManager

职责：

```text
播放按钮音效；
播放出牌音效；
播放吃碰杠胡音效；
控制音量；
开关音效；
缓存音频资源。
```

### 12.2 音效列表

```text
button.mp3
discard.mp3
chow.mp3
pong.mp3
kong.mp3
win.mp3
lose.mp3
draw.mp3
```

第一版如果没有音频资源，可以预留接口。

---

## 13. 资源管理

### 13.1 牌图

命名：

```text
tile_0.png
tile_1.png
...
tile_N.png
tile_back.png
tile_unknown.png
```

映射文件：

```text
TileAssetMap.ts
```

```ts
export const TILE_ASSET_MAP: Record<number, string> = {
  0: 'textures/tiles/tile_0',
  1: 'textures/tiles/tile_1',
};
```

### 13.2 预制体

必须至少包含：

```text
Tile.prefab
PlayerSeat.prefab
ActionPanel.prefab
ResultPanel.prefab
LoadingMask.prefab
Toast.prefab
```

### 13.3 对象池

TileView 会频繁创建销毁，建议使用对象池：

```text
TileViewPool
```

用途：

```text
手牌刷新；
弃牌刷新；
回放播放；
动画生成临时牌。
```

---

## 14. 网络模块

### 14.1 HttpClient

职责：

```text
封装 GET / POST；
自动拼接 BASE_URL；
自动携带 token；
统一错误处理；
支持超时；
支持 mock 模式。
```

### 14.2 WsClient

职责：

```text
连接；
断开；
发送消息；
监听消息；
自动重连；
心跳；
重连后重新订阅房间；
重连后拉取最新 view。
```

状态机：

```text
IDLE
CONNECTING
CONNECTED
RECONNECTING
DISCONNECTED
ERROR
```

### 14.3 重连策略

```text
首次断开立即重连；
后续 1s、2s、4s、8s、10s；
最大间隔 10s；
重连成功后重新订阅 room；
重新获取最新 PlayerGameView。
```

---

## 15. GameManager 设计

文件：

```text
assets/scripts/game/GameManager.ts
```

职责：

```text
保存当前 PlayerGameView；
保存当前 roomId / gameId；
提交动作；
处理 GAME_VIEW；
处理 GAME_EVENTS；
维护 submitting 状态；
维护 selectedTile；
处理终局；
通知 GameBoardView 刷新。
```

关键接口：

```ts
class GameManager {
  currentView: PlayerGameView | null;
  selectedTile: number | null;
  submitting: boolean;

  enterGame(roomId: string, gameId: string): Promise<void>;
  setView(view: PlayerGameView): void;
  selectTile(tile: number | null): void;
  submitAction(action: GameAction): Promise<void>;
  submitDiscard(tile: number): Promise<void>;
  getLegalDiscardTiles(): number[];
  handleWsMessage(message: WsMessage): void;
  leaveGame(): void;
}
```

---

## 16. Mock 模式

为了方便 Codex 和客户端独立开发，必须支持 mock 模式。

### 16.1 Mock 配置

```ts
export const AppConfig = {
  USE_MOCK_HTTP: true,
  USE_MOCK_WS: true,
};
```

### 16.2 Mock 数据

```text
assets/scripts/mock/fixtures/room.json
assets/scripts/mock/fixtures/game-view.json
assets/scripts/mock/fixtures/game-events.json
assets/scripts/mock/fixtures/replay.json
```

### 16.3 MockWsClient

模拟：

```text
ROOM_SUBSCRIBE；
GAME_ACTION；
GAME_VIEW 推送；
GAME_EVENTS 推送；
ERROR；
断线重连；
终局。
```

Mock 模式的目标：

```text
1. 无后端也能打开游戏；
2. 能看到完整牌桌；
3. 能点击手牌；
4. 能模拟出牌；
5. 能模拟 AI 行动；
6. 能进入结算。
```

---

## 17. 微信小游戏发布注意事项

### 17.1 构建目标

Cocos Creator 中选择：

```text
平台：微信小游戏
```

构建后使用微信开发者工具打开构建目录。

### 17.2 包体与资源

小游戏对首包体积敏感，因此建议：

```text
1. 核心脚本进首包；
2. 基础牌图进首包；
3. 高级皮肤、音效、特效走远程资源；
4. 使用图集减少 draw call；
5. 压缩 PNG；
6. 音频尽量短小；
7. 回放资源按需加载。
```

### 17.3 合法域名

上线前需要配置：

```text
request 合法域名；
socket 合法域名；
downloadFile 合法域名；
uploadFile 合法域名。
```

生产环境必须使用：

```text
HTTPS
WSS
```

---

## 18. 与后端和训练侧的边界

### 18.1 与后端

客户端依赖后端：

```text
登录；
房间；
状态；
动作校验；
结算；
牌谱；
AI 行动结果。
```

客户端不绕过后端。

### 18.2 与训练侧

客户端不直接接触训练模型。

AI 流程是：

```text
客户端展示当前轮到 AI
  ↓
后端调用 AI 服务
  ↓
后端拿到 action
  ↓
后端校验并更新状态
  ↓
客户端收到更新后的 GAME_VIEW
```

---

## 19. 测试要求

### 19.1 单元测试

测试：

```text
TileUtils；
ActionUtils；
seat mapping；
GameViewMapper；
Protocol parser；
ReconnectPolicy。
```

### 19.2 Mock 验收

必须能在无后端情况下跑通：

```text
Boot
→ Login
→ Lobby
→ Room
→ Game
→ 点击出牌
→ 模拟 AI 出牌
→ Result
→ Replay
```

### 19.3 联调验收

连接真实后端后必须跑通：

```text
微信登录或 mock login；
创建房间；
添加 AI；
开始游戏；
订阅 WebSocket；
展示 PlayerGameView；
提交动作；
接收 GAME_VIEW；
断线重连；
终局结算；
查看回放。
```

### 19.4 真机验收

```text
iOS 微信；
Android 微信；
弱网；
后台切回；
锁屏恢复；
长时间对局；
不同屏幕比例；
低端机性能。
```

---

## 20. Codex 开发顺序

### Step 1：初始化 Cocos 项目结构

```text
创建基础目录；
创建 Boot/Login/Lobby/Room/Game/Result/Replay 场景；
创建 App.ts 和 AppConfig.ts；
创建 EventBus；
创建 Logger；
创建 Storage。
```

### Step 2：实现协议类型

```text
TileId；
GameAction；
PlayerGameView；
RoomView；
WsMessage；
GameEvent；
ScoreResult。
```

### Step 3：实现网络层

```text
HttpClient；
WsClient；
ReconnectPolicy；
AuthApi；
RoomApi；
ReplayApi。
```

### Step 4：实现全局 Manager

```text
AuthManager；
RoomManager；
GameManager；
AudioManager；
AssetManager。
```

### Step 5：实现基础 View

```text
TileView；
HandView；
DiscardView；
MeldView；
PlayerSeatView；
ActionPanelView；
GameBoardView；
ResultPanelView。
```

### Step 6：实现 Game Scene

```text
GameController；
GameBoardView 绑定；
WebSocket 订阅；
GAME_VIEW 渲染；
GAME_ACTION 提交；
ActionPanel 响应；
Result 跳转。
```

### Step 7：实现 Mock 模式

```text
MockHttpClient；
MockWsClient；
fixtures；
mock game loop。
```

### Step 8：联调后端

```text
关闭 mock；
连接本地 backend；
登录；
创建房间；
添加 AI；
开始游戏；
提交动作；
接收推送。
```

### Step 9：补充动画和音效

```text
出牌动画；
选牌动画；
碰杠胡提示；
结算动画；
按钮音效。
```

### Step 10：微信小游戏构建

```text
构建微信小游戏；
微信开发者工具打开；
配置合法域名；
真机预览；
弱网测试。
```

---

## 21. 最小可运行闭环

第一版必须完成：

```text
1. Boot 场景启动；
2. Mock Login；
3. 进入 Lobby；
4. 创建房间；
5. 添加 3 个 AI；
6. 开始游戏；
7. 进入 GameScene；
8. 渲染自己的手牌；
9. 渲染对手手牌数量、弃牌、明牌；
10. 显示 legalActions；
11. 点击手牌出牌；
12. 后端或 Mock 推送 GAME_VIEW；
13. 更新牌桌；
14. 模拟 AI 行动；
15. 终局进入 Result；
16. 可进入 Replay。
```

---

## 22. 与 Taro 小程序路线的区别

### 22.1 Taro 小程序路线

适合：

```text
页面型应用；
表单、列表、房间页；
轻量交互；
业务系统。
```

不适合：

```text
复杂牌桌动画；
游戏循环；
音效和特效；
大量节点动画；
游戏式表现。
```

### 22.2 Cocos 微信小游戏路线

适合：

```text
麻将；
扑克；
棋类；
休闲游戏；
需要动画和音效的 2D 游戏；
需要游戏场景和资源管理的项目。
```

因此本项目应使用：

```text
Cocos Creator + TypeScript
```

而不是继续使用：

```text
Taro + React
```

---

## 23. 给 Codex 的一句话任务

请基于本文档，在 `game-client/` 或 `frontend/` 文件夹中搭建一个 Cocos Creator 3.x + TypeScript 的微信小游戏客户端：实现 Boot、Login、Lobby、Room、Game、Result、Replay 场景；实现 TileView、HandView、PlayerSeatView、ActionPanelView、GameBoardView 等核心牌桌组件；通过 HTTP 和 WebSocket 对接后端；第一版支持 MockHttpClient 和 MockWsClient 跑通完整麻将游戏闭环，后续再接入真实后端和 AI 对局。

---

## 24. 最终一句话总结

当前项目应按“游戏客户端”而不是“页面小程序”推进：

```text
Cocos Creator 3.x + TypeScript
负责游戏表现、动画、音效和输入；

Node.js 后端
负责房间、规则、状态、结算和牌谱；

Python AI 服务
负责模型推理；

Python 训练仓库
负责模型训练和迭代。
```

客户端永远只展示后端返回的玩家视角，不进行规则裁决，不直接调用 AI，不保存完整暗牌状态。
